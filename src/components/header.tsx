import styled from "styled-components"

export const Header = () => {
  return <Container>Status Services Health</Container>
}
const Container = styled.header`
  display: flex;
  background:grey;
  justify-content: center;
  padding: 16px 0;
`